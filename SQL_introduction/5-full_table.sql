-- create table if not exists
DROP TABLE IF EXISTS first_table

CREATE TABLE IF NOT EXISTS first_table (
	id int NOT NULL AUTO_INCREMENT,
	name varchar(256) DEAULT NULL,
	PRIMARY KEY (d)
)

SELECT * FROM first_table
